<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.acca.api.user.dao.AppUserCouponDao">

    <sql id="userCouponColumns">
		a.id AS "id",
		a.user_id AS "userId",
		a.coupon_id AS "couponId"
	</sql>

    <sql id="userCouponJoins">
    </sql>

    <select id="get" resultType="com.thinkgem.jeesite.acca.api.user.entity.AppUserCoupon">
        SELECT
        <include refid="userCouponColumns"/>
        FROM tbl_user_coupon a
        <include refid="userCouponJoins"/>
        WHERE a.id = #{id}
    </select>

    <select id="findList" resultType="com.thinkgem.jeesite.acca.api.user.entity.AppUserCoupon">
        SELECT
        <include refid="userCouponColumns"/>
        FROM tbl_user_coupon a
        <include refid="userCouponJoins"/>
        <where>

        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
            </otherwise>
        </choose>
    </select>

    <select id="findAllList" resultType="com.thinkgem.jeesite.acca.api.user.entity.AppUserCoupon">
        SELECT
        <include refid="userCouponColumns"/>
        FROM tbl_user_coupon a
        <include refid="userCouponJoins"/>
        <where>
        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
            </otherwise>
        </choose>
    </select>

    <insert id="insert">
		INSERT INTO tbl_user_coupon(
			id,
			del_flag,
			user_id,
			coupon_id
		) VALUES (
			#{id},
			#{delFlag}
			#{userId},
			#{couponId}
		)
	</insert>

    <update id="update">
		UPDATE tbl_user_coupon SET
		    id =                #{id},
		    del_flag =          #{delFlag},
			user_id =           #{userId},
			coupon_id =         #{couponId}
		WHERE id =	  #{id}
	</update>

    <update id="delete">
		UPDATE tbl_coupon SET
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>

</mapper>